#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t{};
    cin >> t;
    while (t--)
    {
        string s{};
        cin >> s;
        int sort_prefix = -1; // unknown initiallly
        for (auto c : s)
        {
        }
    }
}
